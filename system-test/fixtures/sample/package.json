{
  "name": "texttospeech-sample-fixture",
  "description": "An app we're using to test the Texttospeech library.",
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "start": "node build/src/index.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "texttospeech": "file:./texttospeech.tgz"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "typescript": "^3.0.0",
    "gts": "^1.0.0"
  }
}
